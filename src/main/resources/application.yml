server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  application:
    name: bank-cards

  datasource:
    url: jdbc:postgresql://localhost:6543/bank_rest
    username: root
    password: qwerty
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: public

  liquibase:
    change-log: classpath:db/migration/master.yaml
    enabled: true

  security:
    user:
      name: admin
      password: admin

# JWT Configuration
jwt:
  secret: YbSS/0YQOmauYLHwkyqhQaM0fuGexnIFni3tNG3bpLSSCykNUcART4vHTJWNNdxMTDdOo5R0y+b3gbaxKSlHkQ==

# Application specific settings
app:
  encryption:
    key: ${ENCRYPTION_KEY:myEncryptionKey123456789012345678901234567890}

# Logging
logging:
  level:
    com.example.bankcards: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    liquibase: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
